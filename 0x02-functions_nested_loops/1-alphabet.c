#include "main.h"

/**
 * main -  prints form a to z
 * Return: Always 0 (Success)
 */
int main(void)
{
	_putchar('_');
	for(char i ='a'; i >= 'z';i++)
	{
		_putchar(i);
		_putchar('\n')
	}
	return (0);
}
